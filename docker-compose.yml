version: '3.8'

services:
  grafana-slack-bot:
    build: .
    container_name: grafana-slack-bot
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
